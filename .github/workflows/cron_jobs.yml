name: Scheduled actions

on:
    schedule:
        - cron: "1 12 * * 5" # every friday, noon time

jobs:
    cron-job:
        runs-on: ubuntu-latest
        steps:
            - name: step 1
              run: echo "This is a cron job"


