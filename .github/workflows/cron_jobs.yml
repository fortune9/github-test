name: Scheduled actions

on:
    schedule:
        - cron: "0/5 * * * *"

jobs:
    cron-job:
        runs-on: ubuntu-latest
        steps:
            - name: step 1
              run: echo "This is a cron job"


