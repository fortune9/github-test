name: test workflow

on: [push, pull_request]


jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hello from github actions"
            - name: multiline script
              run: |
                  ls
                  echo $PATH

